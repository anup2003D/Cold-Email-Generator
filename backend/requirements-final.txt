# Production-Ready Requirements (No C++ Build Tools Required)
# Works on Python 3.11-3.14 without compilation

# Core FastAPI and Web Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-dotenv>=1.0.0

# Google Authentication and Gmail API
google-auth>=2.27.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0
google-api-python-client>=2.115.0

# LangChain for AI/LLM
langchain-community>=0.0.20
langchain-core>=0.1.0
langchain-groq>=0.0.1

# Vector Database (using DuckDB backend - no C++ compilation needed)
chromadb>=0.4.22

# NLP and Embeddings
sentence-transformers>=2.2.0

# Data Processing
pandas>=2.0.0
# numpy  # Not explicitly needed - pandas will install compatible version

# PDF Processing
PyPDF2>=3.0.0

# HTTP Requests
requests>=2.31.0
httpx>=0.24.0

# Note: hnswlib removed - ChromaDB uses DuckDB backend instead
# This provides the same functionality without requiring C++ build tools
